package encoding

// DB has a File ID which is a UUID
// this UUID is also sharded
// that sharded UUID has stuff like UUID-INDEX.plm, etc.

// we can use smth like that so the base artifact sharding
// deosnt get cooked!
